import React from 'react';
import ReactDOM from 'react-dom';
import './style.scss';
import {EndlessApp} from './endless';

ReactDOM.render(
  <React.StrictMode>
    <EndlessApp />
  </React.StrictMode>,
  document.getElementById('root')
);

